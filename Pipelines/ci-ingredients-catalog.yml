name: CI - IngredientsCatalog

on:
  pull_request:
    paths:
      - "src/backend/IngredientsCatalog/**"
      - "src/backend/IngredientsCatalog.UnitTests/**"
  push:
    branches: [main]
    paths:
      - "src/backend/IngredientsCatalog/**"
      - "src/backend/IngredientsCatalog.UnitTests/**"

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO dotnet restore/build/test after project initialization"
