name: CI - SalviaClients

on:
  pull_request:
    paths:
      - "src/backend/SalviaClients/**"
      - "src/backend/SalviaClients.UnitTests/**"
      - "docs/architecture/backend/**"
  push:
    branches: [main]
    paths:
      - "src/backend/SalviaClients/**"
      - "src/backend/SalviaClients.UnitTests/**"

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO dotnet restore/build/test after project initialization"
