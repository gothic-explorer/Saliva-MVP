# Deployment

- same Salvia source code for all clients;
- independent deployment per client;
- separate DB per client;
- central IngredientsCatalog deployed independently;
- build once, deploy many;
- no branch per client.
