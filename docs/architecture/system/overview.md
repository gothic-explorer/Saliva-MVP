# System Overview

```text
Frontend (TBD)
    |
    v
SalviaClients
    |
    v
IngredientsCatalog
```

Databases:

- one PostgreSQL DB per Salvia client;
- one separate central PostgreSQL DB for IngredientsCatalog.
