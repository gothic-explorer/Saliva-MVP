# Frontend API Integration

The frontend calls SalviaClients.

```text
Frontend -> SalviaClients REST/OpenAPI
```

Direct DB access is forbidden.

Direct IngredientsCatalog access should normally be avoided.
